!!!
- @user = User.find_by(email: @email) || User.find_by(unconfirmed_email: @email)
- @profile_url = profile_url(@user)
- @user_group_url = new_user_group_url
%html
  %head
    %meta{ content: 'text/html; charset=UTF-8', 'http-equiv' => 'Content-Type' }
  %body
    %h1
      - if @user.profile.full_name_or_username.strip.present?
        Welcome to User-Group List, #{@user.profile.full_name_or_username.strip}
      - else
        Welcome to User-Group List, #{@email}
    %p You can confirm your account email #{@email} through the link below:
    %p= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token)
    %p
      To sign in to UGl.st, just follow this link: #{@profile_url}.
    %h2 Register a User-Group Today!
    %p
      User-Group List is built by people like you posting their favorite User-Group's info. Sign in and help grow your
      favorite UG's presence!
    %p You don't have to be the main organizer of a User-Group to list a User-Group. You can enter as many as you like.
    %p
      Don't wait!
      %a{href: @user_group_url} Register a new User-Group now!
    %h2 Thanks for joining and have a great day!
    %p Sincerely,
    %em Mike, creator of UGl.st
    %br
    %br
    %p
      %em p.s. This email goes to a live inbox for Mike. Feel free to reply with any questions, ideas, or concerns.
