<!DOCTYPE html>
<html lang="en">
  <%= render 'layouts/head' %>
  <body>
    <div class="container">
      <div class="container" id="wrap">
        <div class="row">
          <div class="col-md-6 col-md-offset-3">
            <%= render partial: 'layouts/flash' %>
          </div>
        </div>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
